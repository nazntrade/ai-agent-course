"""Day 16 MCP agent project package metadata.

The server name and version live here so both the tool implementations and the
server assembly use a single source of truth.
"""

SERVER_NAME = "day-16-mcp-server"
SERVER_VERSION = "1.0.0"
